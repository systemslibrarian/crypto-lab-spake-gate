Before writing any code, read CRYPTO-LAB-TEMPLATE.md and prompt.txt in the repo
root, in full. Both are binding. Do not begin until you have read both.

Build a new Crypto Lab browser demo (Vite + TypeScript, static site, no backend).

Read CRYPTO-LAB-TEMPLATE.md in full and treat it as the BINDING spec. Build to
every standard in it, in this order:

  1. §1 Build — real crypto only (WebCrypto or a named, justified library; hand-roll
     the inspectable teaching parts; NEVER simulate or fake math). Runnable tests that
     actually pass, including spec KATs (state the count). Mount content at id="app";
     define --accent on :root.
  2. §3 Look — apply the shared top bar and the standardized hero (short-name <h1> +
     spec subtitle + "Why it matters" box beside it; title size capped at
     clamp(1.6rem,3.8vw,2.7rem)); theme contract; scripture footer; head/favicon.
     Do NOT hand-build a header or theme toggle — the shared bar owns those.
  3. §2 Teach — SHOW the one headline mechanism (animate/step it, never assert it in
     prose or raw hex); add a plain-language "what is this / why it matters" intro and a
     break-it-yourself interaction against the real crypto; no decorative/idle animation;
     pitch to a college newcomer while rewarding an expert (progressive disclosure).
  4. §4 Accessibility — wire the WCAG 2.1 AA gate and author to its checklist.
     `npm run build` then `npm run test:a11y` MUST pass with zero violations in BOTH themes.
  5. §5 README (the standard sections) and §6 Deploy (Actions-based Pages, a11y-gated).

Hard rules: do NOT dumb down the crypto to make a visual simpler; honest scoping in-page
and in the README ("not production", what's real vs simulated, what it does NOT prove).
When done, report a one-line summary with the test count.

ADDITIONAL BINDING CONSTRAINTS FOR THIS LAB (this is a "system fails, primitive holds"
lab — these are non-negotiable and outrank feature completeness):

  A. VERDICT SEPARATION. Cryptographic result and security verdict are SEPARATE,
     independently-rendered indicators, never collapsed. "Signature: valid ✓" next to
     "Verdict: REJECT ✗" is the point of the lab. Per §1 VISUAL SEMANTICS, color tracks
     system integrity, never the raw return value: a forged-but-accepted result renders
     as ALARM, not green.
  B. NO PRECISION DRIFT. Every consequence claim must be exactly true for the exact
     construction. If the brief distinguishes authentication-key recovery from
     encryption-key recovery, that distinction must survive into the UI copy verbatim.
     A slightly-overstated attack claim is a §0.2 honesty violation and fails the build.
  C. SCOPE GUARD. The Non-goals list is binding. If a stage is interesting but doesn't
     serve the "Concept to teach", cut it and add a one-line "what this isn't" note per
     §1 SCOPE. When something on the cut list is genuinely adjacent, link to the sibling
     demo — do not rebuild it.
  D. THE LEARNER CAUSES THE FAILURE. The break-it-yourself interaction must run against
     the real primitive and the real verifier. No warning banner substitutes for a button
     that makes genuine crypto produce a genuinely wrong outcome.
  E. DEPENDENCY HALT. If prompt.txt names a module, spec, or sibling repo that this lab
     must CONSUME rather than reimplement, consume it. If that dependency does not yet
     exist or cannot be resolved, STOP and report — do not fork a second implementation
     of it, and do not substitute a stub. If prompt.txt names no such dependency, this
     constraint does not apply.

DEMO BRIEF: read prompt.txt in the repo root and treat its contents as the
NEW DEMO BRIEF block for this build.